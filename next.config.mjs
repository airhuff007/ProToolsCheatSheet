/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable static export for fully offline deployment:
  // output: "export",
  pageExtensions: ["tsx", "ts", "jsx", "js", "md", "mdx"],
};

export default nextConfig;
